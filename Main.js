// Create Evolution's client instance:
const Evolution = require("./src/Evolution");
new Evolution();

console.log("Info: starting...");

// Required modules to launch the bot: discord.js, fs, request.